# Configuration for Financial QA RAG System

# Data paths
data:
  raw_dir: "data/raw"
  processed_dir: "data/processed"
  qa_dir: "data/qa"
  index_dir: "data/indices"
  
  # SEC filings
  sec:
    email: "mehek.ruhma@example.com"  
    companies: ["AAPL"]
    filing_types: ["10-K", "10-Q"]
    years: [2022, 2023, 2024]
    
  # fomc:
  #   years: [2020, 2021, 2022, 2023, 2024]
    
  # Chunking
  chunk_size: 800
    chunk_overlap: 0.2

# Retrieval
retrieval:
  dense:
    model_name: "BAAI/bge-base-en-v1.5"
    top_k: 20

# Hardware
device: "auto"  # "auto", "cuda", "mps", or "cpu" (auto detects best available)
